# Test1
This is the simplest test of LUTE functionality and runs only the basic test Tasks.

Marked as `SHOULD_FAIL` because of the `BinaryErrTester` **managed** `Task`. All other steps should succeed.
